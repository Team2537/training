package Unit2.Lesson2_7

/*
- Create a singleton called `President` with the following properties:
    - `name` (String)
    - `age` (Int)
    - `country` (String)
- Print the `toString()` of the singleton
*/

fun main() {
    // Place your code here
}